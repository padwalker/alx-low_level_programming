#include "main.h"

/**
 * mul - performs multiplication of two numbers
 *
 * @a: first integer
 * @b: second number
 * Return: Always success
 */
int mul(int a, int b)
{
	int result;
	
	result = a * b;
	return (result);
}
